export ANT_HOME=`find ~/Projects -maxdepth 1 -and -name "apache-ant*" -print -quit`
export PATH=$ANT_HOME/bin:$PATH
CLASSPATH=.
for i in ~/Projects/junit/*.jar
do
    CLASSPATH=$CLASSPATH:$i
done
for i in `pwd`/external-jars/*.jar
do
    CLASSPATH=$CLASSPATH:$i
done
export CLASSPATH

