#
# Testing new looping construct
#
:echo "Tests of the new LOOP construct"
:echo

:clear [i, j, value, array, SIZE ]
:dec 50
value = 1
array = []
SIZE = 5
loop $i in SIZE {
   loop $j in SIZE {
      array[$i-1][$j-1] = value++!
   }
}
array;j

:resultonly on
loop $i in 2 .. 102 { isprime($i) ? `${$i} is prime` : "" }
:resultonly previous

:echo
:echo "Done with LOOP tests."

