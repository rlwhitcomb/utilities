:quiet on
define ratphi(n) = { fib(n+1) / fib(n) }
PHI_VALUES = [ ]
loop over 2..400 {
  :dec __
  loop n in __ * 4 {
    if (ratphi(n) == phi) {
      if isnull(PHI_VALUES[__]) {
        PHI_VALUES[__] = n
        leave n
      }
    }
  }
}
:quiet pop
PHI_VALUES@j
