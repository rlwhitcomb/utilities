:quiet on
define ratphi(n) = { fib(n+1) / fib(n) }
PHI_VALUES = [ ]
loop precision over 2..400 {
  settings.precision = precision
  loop n in precision * 4 {
    if (ratphi(n) == phi) {
      PHI_VALUES[precision] = n
      leave
    }
  }
}
:quiet pop
PHI_VALUES@j
