:quiet on
:resultsonly off
start = 11!
end = start + 99999
primes = []
:echo `Running primality tests on ${(end-start)+1} values from $start to $end...`
loop _i in start .. end { if isprime(_i) { primes[length(primes)] = _i } }
:quiet pop
length(primes); primes;

